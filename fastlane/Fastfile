opt_out_usage
lane :beta do
  upload_to_testflight(
    apple_id: ENV["vijaypescs@gmail.com"],
    skip_waiting_for_build_processing: true,
    ipa: "build/IPA/SampleAI.ipa",           # ← Matches your IPA path
    distribute_external: true,
    groups: ["Beta testers"]            # ← Your group name
  )
end